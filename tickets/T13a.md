# T13a — cross-platform import file quota

Depends-on: T12

Allowed-paths:
  - tickets/T13a.md
  - tickets/T13.md
  - internal/repository/import.go
  - internal/repository/import_security_test.go

Forbidden-paths: everything else

Allowed-deps: none

Public API: none

Rules:
- Enforce the configured import byte ceiling on Linux and Darwin despite their different `/bin/sh` `ulimit -f` units.
- Keep the live aggregate-size monitor and final usage validation as defense in depth.
- Do not weaken the hard-quota assertion or change the configured production limit.

Done:
- Exact-boundary block calculations are table-tested for Linux and Darwin units.
- `TestImportHardFileQuota` passes repeatedly on Darwin.
- `make verify` is green.

Blocked-if: enforcing the limit requires a new dependency or exported interface
