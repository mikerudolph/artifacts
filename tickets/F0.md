# F0 — repo scaffold

Depends-on: none

Allowed-paths:
  - go.mod
  - go.sum
  - Makefile
  - Dockerfile
  - docker-compose.yml
  - .gitignore
  - .golangci.yml
  - .testcoverage.yml
  - .mise.toml
  - .github/workflows/ci.yml
  - AGENTS.md
  - tickets/**
  - cmd/artifacts/**
  - internal/types/**
  - internal/config/**
  - internal/store/object/**
  - internal/store/meta/**

Forbidden-paths: everything else

Allowed-deps: none (stdlib only)

Public API: freeze `internal/types`, `object.Store`, `meta.Store`

Done:
  - `make verify` is green
  - package + total coverage ≥ 85%
  - tickets T1–T13 exist

Blocked-if: you need a third-party module
