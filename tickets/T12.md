# T12 — command wiring and e2e

Depends-on: T8, T9, T10, T11

Allowed-paths:
  - cmd/artifacts/**
  - README.md
  - internal/app/**

Forbidden-paths: package internals except `internal/app/**`

Allowed-deps: none new

Public API:

```
artifacts serve
artifacts migrate
artifacts token create
```

Rules:
- `serve` loads config, migrates (or requires migrate), mounts REST + git HTTP on one handler.
- `token create` prints a bootstrap control-plane token (hash stored if postgres is up; or documents `ARTIFACTS_API_TOKEN`).
- README: local postgres compose, `ARTIFACTS_STORAGE=fs`, create repo via curl, git push/clone.
- E2E test (skip without docker+git): create repo over HTTP, push, clone, read file route.

Done:
  - `make verify`
  - README quickstart works on a clean checkout

Blocked-if: a Depends-on ticket is unfinished
