# T13 — coverage close-out

Depends-on: T12

Allowed-paths: any test file (`*_test.go` only)

Forbidden-paths: production code unless a test cannot be written without a tiny export, in which case report BLOCKED first

Allowed-deps: none

Rules:
- Raise any package below 85% to ≥ 85%.
- Total ≥ 85%.
- File threshold 70% remains.
- Do not add tautological tests. Prefer error paths and untested branches.
- Run `make coverage` and attach the report.

Done:
  - `make verify` green
  - no package under 85% except `migrations` (excluded)

Blocked-if: a production change is required to make a package testable
